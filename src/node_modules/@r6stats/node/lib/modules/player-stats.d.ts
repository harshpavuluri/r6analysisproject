import { Platform } from '../types/stats/meta.type';
export declare const playerStats: (username: string, platform: Platform) => {
    method: string;
    url: string;
};
export declare const operatorStats: (username: string, platform: Platform) => {
    method: string;
    url: string;
};
export declare const seasonalStats: (username: string, platform: Platform) => {
    method: string;
    url: string;
};
export declare const weaponStats: (username: string, platform: Platform) => {
    method: string;
    url: string;
};
export declare const weaponCategoryStats: (username: string, platform: Platform) => {
    method: string;
    url: string;
};
